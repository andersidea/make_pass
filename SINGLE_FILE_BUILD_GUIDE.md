# 단일 파일 빌드 가이드 (CORS 문제 해결)

**작성일:** 2025년 1월  
**버전:** v1.5.3  

---

## 📋 개요

CORS 문제를 해결하기 위해 단일 파일 빌드로 전환했습니다. 이제 `dist/index.html` 파일 하나만으로 앱을 실행할 수 있으며, 별도의 서버나 추가 파일이 필요하지 않습니다.

---

## ✅ 변경 사항

### 1. 플러그인 설치

```bash
npm install vite-plugin-singlefile --save-dev
```

### 2. Vite 설정 수정

**변경 파일:** `vite.config.js`

**주요 변경사항:**
- `vite-plugin-singlefile` 플러그인 추가
- `manualChunks` 제거 (단일 파일이므로 불필요)
- `assetsInlineLimit: 100000000` 설정 (모든 자산을 Base64로 인라인)
- `chunkSizeWarningLimit: 10000` 증가 (단일 파일이므로 크기가 큼)

### 3. 빌드 결과

**이전 (멀티 파일):**
```
dist/
├── index.html              # ~1-2 KB
├── assets/
│   ├── index-[hash].js     # ~200-400 KB
│   ├── index-[hash].css    # ~10-20 KB
│   └── ... (여러 벤더 청크 파일)
```

**현재 (단일 파일):**
```
dist/
├── index.html              # ~917 KB (모든 코드 포함)
└── vite.svg                # (참고용, 실제로는 인라인됨)
```

---

## 🚀 사용 방법

### 빌드

```bash
npm run build
```

### 실행

1. **파일 직접 열기**
   - `dist/index.html` 파일을 더블 클릭
   - 브라우저에서 바로 실행 (CORS 문제 없음)

2. **PWA 설치**
   - 브라우저에서 앱 열기
   - "앱 설치" 선택
   - 독립 창으로 실행 가능

---

## 📊 파일 크기

- **압축 전:** ~917 KB
- **gzip 압축:** ~299 KB
- **모든 JavaScript, CSS, 이미지가 HTML에 인라인 포함됨**

---

## ✅ 장점

1. **CORS 문제 해결**
   - 모든 코드가 하나의 HTML 파일에 포함
   - `file://` 프로토콜에서도 정상 작동
   - 서버 없이 바로 실행 가능

2. **배포 간소화**
   - 단일 파일만 배포
   - 사용자가 파일 하나만 받으면 됨

3. **이식성**
   - USB나 이메일로 쉽게 전달 가능
   - 모든 환경에서 동일하게 작동

---

## ⚠️ 주의사항

### 파일 크기

- 단일 파일이므로 파일 크기가 큼 (~917 KB)
- 네트워크를 통한 전송은 gzip 압축 시 ~299 KB로 줄어듦
- 로컬 실행 시에는 파일 크기가 문제되지 않음

### 캐싱

- 단일 파일이므로 전체 앱이 한 번에 로드됨
- 업데이트 시 전체 파일을 다시 다운로드해야 함
- 로컬 실행 환경에서는 문제되지 않음

### 디버깅

- 모든 코드가 하나로 합쳐져 있어 디버깅이 어려울 수 있음
- 개발 환경에서는 여전히 멀티 파일로 실행 (개발 서버 사용)

---

## 🔧 설정 세부사항

### assetsInlineLimit

```javascript
assetsInlineLimit: 100000000  // 100MB
```

- 모든 자산(SVG, 이미지 등)이 Base64로 인코딩되어 HTML에 포함됨
- 100MB까지 인라인 처리하므로 대부분의 자산이 포함됨

### chunkSizeWarningLimit

```javascript
chunkSizeWarningLimit: 10000  // 10MB
```

- 단일 파일이므로 크기가 커도 경고하지 않음
- 실제 파일 크기는 ~917 KB이므로 문제 없음

---

## 🔄 개발 환경

개발 환경에서는 여전히 멀티 파일로 작동합니다:

```bash
npm run dev
```

개발 서버에서는 단일 파일 빌드 플러그인이 적용되지 않으므로, 빠른 HMR(Hot Module Replacement)이 정상적으로 작동합니다.

---

## 📝 빌드 검증

빌드 후 다음을 확인하세요:

- [x] `dist/index.html` 파일이 존재하는가?
- [x] 파일 크기가 900KB 이상인가? (모든 코드 포함 확인)
- [x] `dist/assets/` 폴더가 비어있거나 최소한인가? (대부분 인라인됨)
- [x] `dist/index.html`을 더블 클릭하여 정상 실행되는가?
- [x] CORS 에러가 발생하지 않는가?

---

## 🎯 결론

단일 파일 빌드로 전환하여 CORS 문제를 완전히 해결했습니다. 이제 사용자는 `dist/index.html` 파일 하나만 받아서 더블 클릭만으로 앱을 실행할 수 있습니다.

---

**빌드 명령어:**
```bash
npm run build
```

**실행 방법:**
1. `dist/index.html` 더블 클릭
2. 또는 PWA로 설치

---

**작업 완료일:** 2025년 1월



